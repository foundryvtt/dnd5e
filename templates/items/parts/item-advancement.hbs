<div class="tab details advancement" data-group="primary" data-tab="advancement">
  <ol class="items-list">
    {{#if editable}}
      <li class="items-header flexrow main-controls">
        <div class="item-controls flexrow configuration-mode-control">
          {{#if isEmbedded}}
            {{#if advancementEditable}}
              <a class="item-control" data-action="toggle-configuration"
                title="{{localize 'SHAPER.AdvancementConfigurationActionDisable'}}">
                <i class="fas fa-lock-open"></i> {{localize "SHAPER.AdvancementConfigurationModeEnabled"}}
              </a>
            {{else}}
              <a class="item-control" data-action="toggle-configuration"
                title="{{localize 'SHAPER.AdvancementConfigurationActionEnable'}}">
                <i class="fas fa-lock"></i> {{localize "SHAPER.AdvancementConfigurationModeDisabled"}}
              </a>
            {{/if}}
          {{/if}}
        </div>
        {{#if advancementEditable}}
          <div class="item-controls flexrow add-button">
            <a class="item-control" data-action="add" title="{{localize 'SHAPER.AdvancementControlCreate'}}">
              <i class="fas fa-plus"></i>
            </a>
          </div>
        {{/if}}
      </li>
    {{/if}}

    {{#each advancement as |data level|}}
      <li class="items-header flexrow" data-level="{{level}}">
        <h3 class="item-name flexrow">
          {{#if (eq level "0")}}
            {{localize "SHAPER.AdvancementLevelAnyHeader"}}
          {{else if (eq level "unconfigured")}}
            {{localize "SHAPER.AdvancementLevelNoneHeader"}}
          {{else}}
            {{localize "SHAPER.AdvancementLevelHeader" level=level}}
          {{/if}}
        </h3>

        {{#if (and @root.editable data.configured (ne level "unconfigured"))}}
          <div>
            <a class="item-control" data-action="modify-choices">{{localize "SHAPER.AdvancementModifyChoices"}}</a>
          </div>
        {{/if}}

        {{#if (eq data.configured "full")}}
          <div class="item-checkmark" title="{{localize 'SHAPER.AdvancementConfiguredComplete'}}">
            <i class="fas fa-check-circle"></i>
          </div>
        {{else if (eq data.configured "partial")}}
          <div class="item-warning" title="{{localize 'SHAPER.AdvancementConfiguredIncomplete'}}">
            <i class="fas fa-exclamation-triangle"></i>
          </div>
        {{/if}}
      </li>
      <ol class="item-list">
        {{#each data.items}}
          <li class="advancement-item item flexrow" data-id="{{this.id}}">
            <div class="item-name flexrow">
              <div class="item-image" style="background-image: url('{{this.icon}}')"></div>
              <h4>{{{this.title}}}</h4>
            </div>
            {{#if (or @root.advancementEditable (not @root.isEmbedded))}}
              <div class="flexrow">
                {{#if (eq this.classRestriction "primary")}}
                  {{localize "SHAPER.AdvancementClassRestrictionPrimary"}}
                {{else if (eq this.classRestriction "secondary")}}
                  {{localize "SHAPER.AdvancementClassRestrictionSecondary"}}
                {{/if}}
              </div>
            {{/if}}
            {{#if @root.advancementEditable}}
              <div class="item-controls flexrow">
                  <a class="item-control" data-action="edit" title="{{localize 'SHAPER.AdvancementControlEdit'}}">
                    <i class="fas fa-edit"></i>
                  </a>
                  <a class="item-control" data-action="delete" title="{{localize 'SHAPER.AdvancementControlDelete'}}">
                    <i class="fas fa-trash"></i>
                  </a>
              </div>
            {{/if}}
            {{#if this.summary}}
              <div class="item-summary">
                {{{this.summary}}}
              </div>
            {{/if}}
          </li>
        {{/each}}
      </ol>
    {{/each}}

  </ol>
</div>
