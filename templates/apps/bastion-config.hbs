<fieldset>
    <legend>{{ localize "SETUP.Configuration" }}</legend>

    {{!-- Enabled --}}
    {{ formField fields.enabled value=source.enabled input=inputs.createCheckboxInput localize=true }}

    {{!-- Duration --}}
    {{ formField fields.duration value=source.duration localize=true }}
</fieldset>
