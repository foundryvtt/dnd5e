<div>
    {{{ content }}}

    {{#if effects.length}}
    <effect-application class="dnd5e2">
        {{#each effects}}
        <option value="{{ uuid }}"></option>
        {{/each}}
    </effect-application>
    {{/if}}
</div>
