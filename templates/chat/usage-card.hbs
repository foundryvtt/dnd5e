{{#if content}}
{{{ content }}}
{{else}}
{{> "dnd5e.card-face" }}
{{/if}}

{{#if (and effects.length showTargets)}}
<effect-application class="dnd5e2">
    {{#each effects}}
    <option value="{{ uuid }}"></option>
    {{/each}}
</effect-application>
{{/if}}
