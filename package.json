{
  "scripts": {
    "build": "gulp css",
    "build:clean": "gulp cleanPacks",
    "build:db": "gulp compilePacks",
    "build:json": "gulp extractPacks",
    "build:watch": "gulp"
  },
  "devDependencies": {
    "fs": "^0.0.1-security",
    "gulp": "^4.0.2",
    "gulp-less": "^5.0.0",
    "merge-stream": "^2.0.0",
    "nedb": "^1.8.0",
    "path": "^0.12.7",
    "through2": "^2.0.5",
    "yargs": "^7.1.2"
  }
}
